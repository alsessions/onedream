<nav class="bg-white px-6 py-5" x-data="{ open: false }">
    <div class="mx-auto flex max-w-7xl items-center justify-between">
        <a href="/" class="shrink-0" aria-label="One Dream home">
            <img class="h-12 w-auto md:h-14" src="/assets/One-Dream-logo.jpeg" alt="One Dream" width="798" height="543" />
        </a>

        <div class="hidden items-center gap-8 text-lg font-black uppercase tracking-widest text-brand-blue-dark md:flex">
            <a class="hover:underline" href="/menu">Menu</a>
            <a class="hover:underline" href="/about">About</a>
        </div>

        <button
            class="inline-flex size-10 items-center justify-center rounded-md ring-1 ring-stone-300 md:hidden"
            type="button"
            aria-label="Toggle navigation"
            :aria-expanded="open"
            @click="open = !open"
        >
            <span class="sr-only">Toggle navigation</span>
            <span class="flex w-5 flex-col gap-1.5">
                <span class="h-0.5 rounded-full bg-ink"></span>
                <span class="h-0.5 rounded-full bg-ink"></span>
                <span class="h-0.5 rounded-full bg-ink"></span>
            </span>
        </button>
    </div>

    <div class="mx-auto mt-4 max-w-5xl rounded-lg bg-white p-4 shadow-sm ring-1 ring-stone-200 md:hidden" x-cloak x-show="open" @click.outside="open = false">
        <div class="flex flex-col gap-3 text-lg font-black uppercase tracking-widest text-brand-blue-dark">
            <a href="/menu">Menu</a>
            <a href="/about">About</a>
        </div>
    </div>
</nav>
